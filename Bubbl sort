// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
void sort(int arr[], int n){
    int counter = 0;
    while(counter <=  n-1){
        for(int i = 0; i < n-counter; i++){
            if(arr[i]>arr[i+1]){
                int temp = arr[i+1];
                arr[i+1]= arr[i];
                arr[i]= temp;
            } 
        } counter = counter+1;
    } 
    for(int i = 0; i<n ; i++){
        cout<<arr[i]<<" ";
    }
}
int main() {
    int n;
    cin>>n;
    int arr[n];
    for(int i = 0; i<n; i++){
        cin>>arr[i];
    }
    sort(arr,n);
}
