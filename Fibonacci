// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;

void fib(int num){
    
    int t1, temp;
    t1 = temp = 0;
    int t2 = 1;
    cout<<"0 1 ";
    
    for(int i=3 ; i<=num ; i++){
        temp = t2 + t1;
        t1 = t2;
        t2 = temp;
        cout<<t2<<" ";
    }
}
int main() {
    int num;
    cin>>num;
    if(num <= 2){
        cout<<"0 1 ";
    } else{
        fib (num);
    }
    return 0;
}
